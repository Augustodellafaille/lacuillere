
<%= form_for [@restaurant, @review] do |f| %>
<div>
  <%= f.label :content %>
  <%= f.text_area :content %>
</div>
<div>
  <%= f.label :rating %>
  <%= f.number_field :rating %>
</div>
<div class="action">
  <%= f.submit "add a review"%>
</div>
<% end %>

